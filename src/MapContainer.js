import React, { Component } from 'react';

class MapContainer extends Component {
  render() {
    return (
      <div id="map">
        Here is where the map will go
      </div>
    )
  }
}

export default MapContainer;
